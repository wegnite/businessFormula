export declare const lockFiles: {
    readonly npm: "package-lock.json";
    readonly yarn: "yarn.lock";
    readonly pnpm: "pnpm-lock.yaml";
    readonly bun: "bun.lockb";
};
/**
 * Checks if the given filename is a lock file (for any package manager)
 *
 * @param filename the filename to check
 * @returns true if the given filename is that of a lock file, false otherwise
 */
export declare function isLockFile(filename: string): boolean;
//# sourceMappingURL=locks.d.ts.map