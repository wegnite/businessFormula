import { NextResponse } from 'next/server.js';

type CreateNextRouteHandlerOptions = {
    apiUrl?: string;
};
declare function createNextRouteHandler(options: CreateNextRouteHandlerOptions): (req: Request) => Promise<NextResponse<unknown>>;

export { createNextRouteHandler };
